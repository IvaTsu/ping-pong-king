import "./App.css";

function App() {
  return (
    <div className="App">
      <div className="card">
        <button onClick={() => console.log("Login")}>Sign in</button>
      </div>
    </div>
  );
}

export default App;
